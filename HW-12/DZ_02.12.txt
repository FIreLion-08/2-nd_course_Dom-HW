Условия домашки
Чтобы выполнить основную часть домашнего задания, используйте материалы урока – в них вы найдете все необходимое для решения.

 
💡 В этой домашке мы продолжим разрабатывать приложение «Лента комментариев».

В каком виде сдавать работу
Сделайте pull request в свой репозиторий с прошлой домашкой с результатами выполнения этой домашки. На момент сдачи работы PR должен быть открытым.

После того как наставник примет работу, смержите свой пул-реквест в main-ветку.

Задание
Подключите приложение комментариев к API комментариев, в результате приложение должно получать список комментариев из API и добавлять новый комментарий через API.

Обратите внимание, что удаление комментариев через API мы в этой домашке не делаем, т.к. в API у нас нет такого функционала

Документация API
https://github.com/GlebkaF/webdev-hw-api/blob/main/pages/api/v1/[key]/comments/README.md

Подсказки
Проблема: формат данных в API не совпадает с форматом в приложении.

Подсказка
GET API возвращает данные не в том формате, который мы использовали до этого в коде. Например, API возвращает дату в виде строчки, а имя автора лежит внутри свойства author. Такое часто бывает при работе с API, поэтому данные из API нужно «подготовить» для нашего приложения.

Используйте map-функцию на ответе, чтобы преобразовать данные, которые приходят из API, в формат, с которым умеет работать наше приложение.

Пример кода


Проблема: POST API не возвращает новый список комментариев.

Подсказка
Чтобы получить актуальный список, сделайте новый GET-запрос на получение актуального списка после того, как запрос на добавление выполнился.

Как должно работать приложение в результате

Условия проверки и выполнения задания
После перезагрузки страницы список комментариев не сбрасывается:
при запуске приложение загружает список комментариев из API и выводит его на экран;
при добавлении комментария приложение сохраняет комментарий в API, а затем выводит добавленный комментарий в список.
Функционал лайков работает локально, то есть можно поставить и снять лайк, но после перезагрузки страницы счетчик лайков сбросится.
Дополнительное задание
Приступайте к дополнительным заданиям только после того, как закончите с основной частью.

Наставники примут и проверят задание и без дополнительной части, но ее выполнение поможет вам лучше разобраться с темой и упростит понимание следующих и предыдущих тем курса.

Задание
Добавление коммента в API занимает некоторое время, и до тех пор, пока запрос не завершится, пользователь видит странное состояние нашего приложения: он, вроде, нажал на кнопку, форма уже очистилась, а комментарий в списке еще не появился.

Чтобы нагляднее увидеть эту проблему, замедлите интернет через консоль разработчика, на вкладке «Сеть» в выпадающем списке выберете 2G и попробуйте добавить пост:



Наша задача: показать пользователю, что с приложением всё в порядке, просто запрос еще не выполнился. Для этого на время загрузки скроем форму добавления, вместо нее покажем текст «Комментарий добавляется», как на картинке ниже.

Чтобы сделать совсем красиво, вместо текста можно показать анимацию загрузки, ее можно найти по запросу gif loader в гугле.



Подсказка легкая
Так как форма добавления становится динамической, то вам точно понадобится рендер-функция, чтобы рендерить данные по условию.

Подсказка «почти решение»
Перед тем как отправлять запрос на добавление в API, установите в коде флаг isLoading = true, а после того, как запрос завершится, верните его обратно в isLoading = false. Затем в рендер-функции, в зависимости от значения флага, нужно рендерить либо форму, либо индикатор загрузки.